http://www.site.uottawa.ca/~mkhak052/TA-SignalSystemLab/
% Lab 2  % Sine wave: T=6;            % period t=0:0.01:60;    % from 0 to 60, step 0.01 y=sin((2*pi/T)*t); subplot( 3, 3, 1); plot(t, y); axis([0 2*pi -1 1]);% Plot from x: 0-5, y: 0-1  % Exponential: b = exp(t/100); subplot( 3, 3, 2); plot(t, b); %axis([0 5 0 inf]);% Plot from x: 0-5, y: 0-1  % Steps: c = exp(t/100); subplot( 3, 3, 3); stem(t, c); axis([0 0.1 0 inf]);% Plot from x: 0-5, y: 0-1  % Square wave: d = sign( sin(( 2*pi/T)*t)); subplot( 3, 3, 4); plot(t, d); %axis([0 30 -1 inf]);% Plot from x: 0-5, y: 0-1 axis auto  grid; figure(1)  figure(2) e = exp( ((1j)*2*pi/T)*t); plot3(t, real(e), imag(e)); % plot 3D grid; axis square;       az = atan(real(e)/t);     el = atan(imag(e)/t);     view(az, el);  % subplot (nrows,ncols,plot_number)
